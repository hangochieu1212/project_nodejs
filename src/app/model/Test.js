/*const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const Test = new Schema({
    name:   {type: String},
    age:    {type: String},
    class:  {type : String},
    school: {type: String}
  });
  module.exports = mongoose.model('tsets' , Test);
  */
